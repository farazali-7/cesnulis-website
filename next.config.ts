import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",

  // Hides the dev-only Next.js badge in the corner of the viewport.
  devIndicators: false,
};

export default nextConfig;
